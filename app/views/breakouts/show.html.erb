<% if user_signed_in? %>
  <h1><%= @breakout.title %></h1>
  <p><%= @breakout.body %></p>
  <ul>
    <% @breakout.comments.each do |comment| %>
      <li><%= comment.body %></li>
    <% end %>
  </ul>
<% else %>
  <h1>Please sign in to view the details of this breakout.</h1>
<% end %>
